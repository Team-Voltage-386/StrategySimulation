# Extra dependencies for the maple-sim bridge only.
#
# Kept out of the top-level requirements.txt on purpose: running the strategy
# simulator does not require a JVM, a robot project, or WPILib's Python
# bindings, and nobody who just wants the GUI should have to install them.
#
#   pip install -r bridge/requirements.txt

# NetworkTables 4 client. Pin to the robot project's GradleRIO version
# (2026.2.1) -- same NT4 implementation on both sides, not a reimplementation.
pyntcore==2026.2.2

# Sync WebSocket client for the HALSim operator channel.
websocket-client>=1.8.0
